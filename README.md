# ansible-container-rainloop


An Ansible Playbook to Run RAINLOOP in a *Container* 

* Inputs 

```Hosts File```
To set hostname IP within the Container 


```Site.yml``` 
To set Your Mail Configuration to Fetch and Send Email 
`domain_name`
`imap_host`
`imap_port`
`smtp_host`
`smtp_port`



